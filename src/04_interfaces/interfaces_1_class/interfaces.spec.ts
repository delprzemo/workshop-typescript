import { expect } from 'chai';

describe('Interfaces - classes', () => {

    // it('Class implementing interface', () => {

    //     interface IUser {
    //         user: string;
    //         password: string;
    //         login: (user: string, password: string) => boolean;
    //     }
        
    //     class User implements IUser {
    //         // exercise - implement content
    //     }

    //     expect(new User().login("user1", "password1")).to.equal(true);
    // });

    // it('Class inplementing two interfaces', () => {

    //     interface IUser {
    //         user: string;
    //         password: string;
    //         login: (user: string, password: string) => boolean;
    //     }

    //     interface ILogged {
    //         isLogged: boolean;
    //     }
        
    //     class User implements IUser, ILogged {
    //         // exercise - implement content
    //     }

    //     expect(new User().login("user1", "password1")).to.equal(true);
    //     expect(new User().isLogged).to.equal(false);
    // });

    // it('Constructor builder with interface', () => {

    //     interface ComesFromString {
    //         name: string;
    //     }

    //     class MadeFromString implements ComesFromString {
    //         constructor (public name: string) {

    //         }
    //     }
        
    //     // exercise - implement makeObj1 and makeObj2 that will create instances of MadeFromString

    //     expect(makeObj1(MadeFromString).name).to.equal('test');
    //     expect(makeObj2(MadeFromString).name).to.equal('test2');
    // });
});