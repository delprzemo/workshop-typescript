import { expect } from 'chai';

describe('Functional programming - applicative', () => {

    // it('Example', () => {
    //     class Container<T> {
    //         private _value!: T;
        
    //         public constructor(val: T) {
    //             this._value = val;
    //         }

    //         // Exercise - create map, app and of functions 

    //         //only for assert
    //         get value() {
    //             return this._value;
    //         }
    //     }
    
    //     let double = (x: number) => x + x;
    //     let numberContainer = Container.of(3);
    
    //     // Ordinary functors
    //     let newContainer = numberContainer.map(double);
    
    //     // Applicative
    //     let functionContainer = Container.of(double);
    //     let newContainer2 = numberContainer.ap(functionContainer);

    //     expect(newContainer.value).to.equal(6);
    //     expect(newContainer2.value.value).to.equal(6);
    // });
});
