import { expect } from 'chai';

describe('Functional programming optics', () => {

    // interface Lens<T1, T2> {
    //     get(o: T1): T2;
    //     set(o: T2, v: T1): T1;
    // }

    
    // // Intermediate
    // it('Simple immutable object', () => {
    //     expect(1).to.equal(1); 
        
    // });
});
