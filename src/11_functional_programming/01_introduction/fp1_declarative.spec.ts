import { expect } from 'chai';

describe('Functional programming declarative', () => {


    const results = [64, 87, 89];

    // it('Imperative programming', () => {
    //     function avg(arr: number[]) {
    //         let total = 0;
    //         for (var i = 0; i < arr.length; i++) {
    //             total += arr[i];
    //         }

    //         return total / arr.length;
    //     }

    //     expect(avg(results)).to.equal(80);
    // });

    // it('Declarative programming', () => {
    //     //exercise - create avg function in declarative way

    //     expect(avg(results)).to.equal(80);
    // });

});

