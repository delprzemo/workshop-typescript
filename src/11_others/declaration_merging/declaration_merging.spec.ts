import { expect } from 'chai';
// import {getAnimal as AnimalFunction} from './declaration_merging'
// import {color as Color} from './declaration_merging'

describe('Declaration merging', () => {

    // it('Exmaple with interfaces', () => {

    //     interface Box {
    //         height: number;
    //         width: number;
    //     }
        
    //     interface Box {
    //         scale: number;
    //     }
        
    //     let box: Box = {height: 5, width: 6, scale: 10};

    //     expect(box.height).to.equal(5);
    //     expect(box.scale).to.equal(10);
    //     expect(box.width).to.equal(6);
    // });

    // it('Exmaple with namespaces', () => {
    //     expect(AnimalFunction('Test').name).to.equal('Test');
    // });

    // it('Exmaple with mix namespace and enums', () => {
    //     expect(Color.blue).to.equal(3);
    //     expect(Color.mixColor('white')).to.equal(6);
    // });

});

