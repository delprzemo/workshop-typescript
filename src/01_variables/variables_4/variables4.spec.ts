import { expect } from 'chai';

describe('destructuring, spread', () => {

    // it('spread for array', () => {
    //     let first = [1, 2];
    //     let second = [3, 4];
    //     let bothPlus = [0, ...first, ...second, 5];

    //     expect(bothPlus.length).to.equal(/*exercise*/);
    //     expect(bothPlus[2]).to.equal(/*exercise*/);
    // });

    // it('spread for object', () => {
    //     let defaults = { food: "spicy", price: "$$", ambiance: "noisy" };
    //     let search = { food: "rich", ...defaults };

    //     expect(search.price).to.equal(/*exercise*/);
    //     expect(search.food).to.equal(/*exercise*/);
    // });

    // it('destructuring', () => {
    //     let input = [1, 2];
    //     let [first, second] = input;

    //     expect(first).to.equal(/*exercise*/);
    //     expect(second).to.equal(/*exercise*/);
    // });

    // it('destructuring with params', () => {
    //     let [first, ...rest] = [1, 2, 3, 4];

    //     expect(first).to.equal(/*exercise*/);
    //     expect(rest).to.deep.equal(/*exercise*/);
    // });

    // it('destructuring and spreading', () => {
    //     let first = [1, 2];
    //     let second = [3, 4];
    //     let bothPlus = [0, ...first, ...second];
    //     let [firstElement, ...rest] = [...bothPlus];


    //     expect(firstElement).to.equal(/*exercise*/);
    //     expect([...rest]).to.deep.equal(/*exercise*/);
    // });

    // it('restrict parameters for function by custom type', () => {
    //     type C = { a: string, b?: number }
    //     function f({ a, b }: C): any {
    //         return a + b;
    //     }

    //     expect(f({a: 'test', b: 2})).to.equal(/*exercise*/);
    // });
});