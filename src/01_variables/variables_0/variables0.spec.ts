import { expect } from 'chai';

describe('Variables declaration and assignment', () => {

    // it('nums has number type and proper value', () => {
    //     var numWithValue = 1;
    //     var numWithoutValue: number;

    //     expect(typeof numWithValue).to.equal(/*exercise*/);
    //     expect(numWithValue).to.equal(/*exercise*/);

    //     expect(typeof numWithoutValue).to.equal(/*exercise*/);
    //     expect(numWithoutValue).to.equal(/*exercise*/);
    // });
});

