import { expect } from 'chai';


// exercise create class Variables which will pass tests and will have one readonly parameter

describe('Class - properties declaration', () => {
    // var variables = new Variables();
    
    // it('properties type checking', () => {
    //     expect(typeof variables.numWithValue).to.equal('number');
    //     expect(variables.numWithValue).not.to.equal(undefined);

    //     expect(typeof variables.numWithoutValue).to.equal('undefined');
    //     expect(variables.numWithoutValue).to.equal(undefined);
    // });

    // it('readonly property', () => {
    //     //variables.readonlyValue = 3;

    //     expect(typeof variables.readonlyValue).to.equal('number');
    //     expect(variables.numWithValue).not.to.equal(undefined);
    // });
});