import { expect } from 'chai';

describe('Iterators', () => {

    // it('Iterators  - simple itarator with let..of', () => {

    //     let someArray = [1, "string", false];
    //     let newArray = [];

    //     for (let entry of someArray) {
    //         newArray.push(entry);
    //     }

    //     expect(someArray).to.deep.equal(/*Exercise*/);
    // });

    // it('Iterators  - simple itarator with let..in', () => {

        
    //     let someArray = [1, "string", false];
    //     let newArray = [];

    //     for (let entry in someArray) {
    //         newArray.push(entry);
    //     }

    //     expect(newArray).to.deep.equal(/*Exercise*/);
    // });

    // it('Iterators  - simple itarator with let..in', () => {

    //     let cat = {
    //         name: 'Cat',
    //         sound: 'Miau'
    //     }

    //     let newArray = [];

    //     for (let catProperty in cat) {
    //         newArray.push(catProperty);
    //     }

    //     expect(newArray).to.deep.equal(/*Exercise*/);
    // });
});

