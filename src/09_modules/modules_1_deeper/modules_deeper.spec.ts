import { expect } from 'chai';

/*Exercise - override global require function here*/
/*Exercise - import url*/

/*Uncommnet later*/
declare const System: any;


describe('Modules - deeper', () => {

    // it('Simple usage of optional custom require', () => {

    //     let length = require('./modules_deeper_sample');

    //     expect(length('12345')).to.equal(true);
    //     expect(length('123456')).to.equal(false);
    // });

    // it('Simple usage aliased module', () => {

    //     let myUrl = URL.parse("http://www.typescriptlang.org");

    //     expect(typeof URL.parse).to.equal('function');
    // });

    /*Uncommnet later*/
    // (Javascript has this library but typescript doesn't see)
    // it('Deeper usage for System', () => {
    //     System.import('./modules_deeper_sample').then((result: any) => {
    //         expect(result).to.equal(true);
    //     })
    // });
});

