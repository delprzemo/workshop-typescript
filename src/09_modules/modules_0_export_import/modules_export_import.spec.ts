// Relative import (there is an alias)
import { expect } from 'chai';

// Non-relative

/*Exercise - import Reposity and lengthChecker */


describe('Modules - export import', () => {

    // it('Simple usage of imported class', () => {

    //     let repo = new Repository();

    //     expect(repo.GetById(1).id).to.equal(1);
    // });

    // it('Simple usage of imported default class', () => {

    //     let repo = new Repository();

    //     expect(lengthChecker("12345")).to.equal(true);
    //     expect(lengthChecker("123456")).to.equal(false);
    // });
});

