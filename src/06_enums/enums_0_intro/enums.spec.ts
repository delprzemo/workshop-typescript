import { expect } from 'chai';

describe('Enums - introduction', () => {

    // it('Simple enum', () => {

    //     /*Exercise - create Direction enum*/

    //     expect(Direction.Up).to.equal(0);
    //     expect(Direction.Down).to.equal(1);
    //     expect(Direction.Left).to.equal(2);
    //     expect(Direction.Right).to.equal(3);
    // });

    // it('Simple enum with default value - two same', () => {

    //     /*Exercise - create Direction enum*/

    //     expect(Direction.Up).to.equal(0);
    //     expect(Direction.Down).to.equal(1);
    //     expect(Direction.Left).to.equal(1);
    //     expect(Direction.Right).to.equal(2);
    // });

    // it('Simple enum  - used in function as parameter', () => {

    //     enum Answers {
    //         No ,
    //         Yes
    //     }

    //     function respond (answers: Answers): boolean {
    //         /*Exercise - implement body*/
    //     }

    //     expect(respond(Answers.No)).to.equal(false);
    //     expect(respond(Answers.Yes)).to.equal(true);
    // });

    // it('String enum', () => {

    //     /*Exercise - create Direction enum*/

    //     expect(Direction.Up).to.equal("UP");
    //     expect(Direction.Down).to.equal("DOWN");
    //     expect(Direction.Left).to.equal("LEFT");
    //     expect(Direction.Right).to.equal("RIGHT");
    // });

    // it('Enum constant and computed', () => {

    //     /*Exercise - make enum const*/
    //     enum FileAccess {
    //         None,
    //         Read    = 1 << 1,
    //         Write   = 1 << 2,
    //         ReadWrite  = Read | Write,
    //         G = "123".length,
    //         F = Read + Write
    //     }
        

    //     expect(FileAccess.None).to.equal(0);
    //     expect(FileAccess.Read).to.equal(2);
    //     expect(FileAccess.Write).to.equal(4);
    //     expect(FileAccess.ReadWrite).to.equal(6);
    //     expect(FileAccess.G).to.equal(3);
    //     expect(FileAccess.F).to.equal(6);
    // });

    // it('Reversed enum', () => {

    //     enum MyEnum {
    //         A, 
    //         B
    //     }
        
    //     expect(/*Exercise*/).to.equal("A");
    //     expect(/*Exercise*/).to.equal("B");
    //     expect(/*Exercise*/).to.equal(1); 
    // });
});

